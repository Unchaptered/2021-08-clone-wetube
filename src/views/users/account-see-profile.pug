extends ../base
include ../mixins/video

block content
    div.seeProfile__box
        img(src="/"+userDB.avatarUrl)
    div.home__videos-grid
        each video in userDB.childVideo
            +video(video)
        else
            li Sorry Nothing found.