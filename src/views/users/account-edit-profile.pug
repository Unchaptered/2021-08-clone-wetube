extends ../base

block content
    if errorMessage
        span=errorMessage
    div.editProfile__img__box
        if loggedInUser.avatarUrl !== "none"
            img(src="/"+loggedInUser.avatarUrl, width="100", height="100").editProfile__img__img
    form(method="POST", enctype="multipart/form-data").editProfile__form
        label(for="avartar") Avartar
        br
        input(name="avartar", type="file", accept="image/*")
        input(name="name", value=loggedInUser.name, placeholder="Name", type="text", required)
        input(name="email", value=loggedInUser.email, placeholder="Email", type="email", required)
        input(name="username", value=loggedInUser.username, placeholder="Username", type="text", required)
        input(name="location", value=loggedInUser.location, placeholder="Location", type="text", required)
        
        input(value="Modify Profile", type="submit")
        br
        if !loggedInUser.socialOnly
            br
            hr
            a(href="edit-password") Change Password &rarr;
